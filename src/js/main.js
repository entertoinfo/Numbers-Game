// Кількість пар які створяться для ігрового поля.
      const gameContainer = document.getElementById("game");
      gameContainer.appendChild(oneZero(15));

      //* Level difficulty
      const eazyLevel = 15;
      const grandfatherLevel = 30;
      const crazyLevel = 50;